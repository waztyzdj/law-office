# 部署说明

本文件记录生产或测试环境部署约定，当前为初版模板。

## 后端部署

1. 构建后端 jar。
2. 准备 JDK 17 运行环境。
3. 配置数据库、Redis、MinIO、JWT 密钥等环境变量或配置文件。
4. 执行数据库脚本。
5. 启动 Spring Boot 服务。

## 前端部署

1. 执行前端构建。
2. 将 `frontend/dist` 部署到静态资源服务器。
3. 配置 API 代理或后端地址。

## 发布前检查

- 数据库脚本已执行。
- 配置密钥来自部署环境。
- 前后端接口地址一致。
- 登录、菜单、权限、用户列表可用。
